Extension of file the connected components in a graph.
- We need to store the shape of the components in a set we are using the set so that we only store the unique shapes.
- the way We store The shapes Press that we Find the shape in a particular dfso bfs traversal And then we subtract the root's coordinates from it.
- Thus he always find out the base shape.